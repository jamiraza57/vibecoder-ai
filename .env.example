# Copy to .env and fill in your own values. Never commit the real .env file.

ANTHROPIC_API_KEY=
# Check Anthropic's current model list before setting this — model ids change over time.
ANTHROPIC_MODEL=

# Reserved for future provider adapters (not yet implemented — see ARCHITECTURE.md Phase 9).
OPENAI_API_KEY=
GOOGLE_API_KEY=

MAX_AGENT_STEPS=50
MAX_REPAIR_ATTEMPTS=5
